#include "MULS_FIXP.inc"
#include "MUL.inc"

module MULS_FIXP{
	MUL mul;
	wire result[38];

	func exe{
		result = mul.exe(a,b).r;
		r = result[34:16];

		if(result == 38'b0) rs = 0;
		else rs = as ^ bs;
	}
}
